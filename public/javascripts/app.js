/* global Handlebars */
/* global moment */

// Handlebars.registerHelper('format', function (text, format) {
//     // text = Handlebars.Utils.escapeExpression(text);
//     // return new Handlebars.SafeString(result);
//     return moment(text).format(format);
// });